[package]
name = "shiguredo_aom_pbt"
version = "0.0.0"
edition = "2024"
rust-version = "1.93"
publish = false

[dependencies]
# 被テスト対象 (ソースビルドで libaom をリンクする)
shiguredo_aom = { path = "..", features = ["source-build"] }

[dev-dependencies]
# Property-Based Testing
noprop = "0.2"
